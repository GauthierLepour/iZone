<h1><%= @event.name %></h1>
<%= cl_image_tag @event.photo.key, height: 300, width: 400, crop: :fill %>
<h3><%= @event.description %></h3>

<%= link_to 'Propose your car', current_user.cars.empty? ? new_car_path : new_event_ride_path(@event) %>
